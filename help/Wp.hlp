
USAGE
        Wp [-acdnrl nb] [callsign]

DESCRIPTION
        Give access to the FPAC white pages database.
options :
         n = Nodes only
         l = List length 
  sort by :
       	 a address
         c callsign (default)
         d date
         r reverse

	 You can may ? and * wildcards in the callsign.
