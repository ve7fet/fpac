
USAGE
        Links [* | <call>]

DESCRIPTION
        Displays a list of active AX.25 connections with the local host.

	An asterisk (*) argument displays AX.25 sockets in listening state.

        With a callsign argument all connections with either source or 
        destination callsign <call> are displayed.
