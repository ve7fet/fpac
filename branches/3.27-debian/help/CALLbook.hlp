
USAGE
        CALLbook <callsign>[@<hostname>]

DESCRIPTION
        Retrieves data about a callsign from an internet-accessible
        callbook server. The hostname can usually be omitted, since
        the default server configured by the operator is usually
        valid. The default setting can be overridden by specifying
        a host name. 

EXAMPLES
        callbook oh7lzb
        callbook oh7lzb@zone.oh7rba.ampr.org
